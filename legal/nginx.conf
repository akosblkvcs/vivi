server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Serve /tos -> tos.html, /privacy -> privacy.html, etc.
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
